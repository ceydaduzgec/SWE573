# Rename the "sample.env.txt" to ".env" file under sole_project and put related values.
# Delete comments

DJANGO_SECRET_KEY=""
DEBUG=True

DB_NAME=
DB_USER=
DB_PASSWORD=
DB_HOST=
DB_PORT=5432
ALLOWED_HOSTS= use this only if you need it in production, otherwise delete it